# Repository state

`repos.tsv` is generated by `bin/dw snapshot`. It records the exact commit,
branch, origin URL, and dirty flag for the Darling superproject and every
initialized submodule.

It is an audit and handoff manifest, not an automatic checkout mechanism.
Darling's gitlinks remain authoritative for clean build pins.

